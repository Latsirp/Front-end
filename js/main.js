const ativarMenuLateral = document.getElementById("ativarMenuLateral"); // Pegando o ID de ativarMenuLateral no HTML
const menuLateral = document.getElementById("menuLateral"); // Pegando o ID de menuLateral no HTML

// Criando um evento que ativará e desativará o menu lateral da página
ativarMenuLateral.addEventListener("click", function () {});
